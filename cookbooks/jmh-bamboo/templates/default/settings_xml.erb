<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
		  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		  xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">
<!-- Dropped Off By Chef! -->
	<servers>
		<server>
			<id>releases-and-snapshots</id>
			<username><%= @archiva_user%></username>
	                <password><%= @archiva_password %></password>
		</server>
		<server>
			<id>jmh-snapshots</id>
			<username><%= @archiva_user%></username>
			<password><%= @archiva_password %></password>
		</server>
		<server>
				<id>jmh-internal</id>
				<username><%= @archiva_user%></username>
				<password><%= @archiva_password %></password>
		</server>
	</servers>

	<mirrors>
		<mirror>
			<id>releases-and-snapshots</id>
			<name>Maven Repository Manager running on mvnrepo.johnmuirhealth.com</name>
	       <url><%= @server_name %></url>
			<mirrorOf>*</mirrorOf>
		</mirror>
	</mirrors>
</settings>